class: Image
table: image

fields:
  id: UuidValue?, defaultPersist=random_v7
  userId: UuidValue, relation(parent=app_user)
  checkId: UuidValue, relation(parent=symptom_check)
  url: String
  metadata: String
  encryptionKey: String
  descriptionKey: String
  uploadedAt: DateTime
