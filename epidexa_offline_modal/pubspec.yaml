name: skinaware_offline_modal
description: Skin disease detection model training and inference with ChatGPT integration
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  http: ^1.1.0
  path: ^1.8.3
  image: ^4.0.17
  tflite_flutter: 0.10.4
  crypto: ^3.0.3
  dio: ^5.4.0

dev_dependencies:
  lints: ^2.1.0
  test: ^1.24.0
